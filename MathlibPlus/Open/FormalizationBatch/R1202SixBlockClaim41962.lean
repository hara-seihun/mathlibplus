import MathlibPlus.Open.FormalizationBatch.R1202SixBlockClaim32191

namespace MathlibPlus.Open.FormalizationBatch.R1202SixBlock

/-- Claim 41962 is the same direct-product six-block obstruction as the
reviewed same-locator Claim 32191.  The supporting declaration supplies the
explicit affine-plane carrier, common left-regular `S₃` action, six blocks,
quotient image, and the two characteristic kernels. -/
def directProductSixBlockObstruction_claim41962 : Prop :=
  directProductSixBlockObstruction_claim32191

end MathlibPlus.Open.FormalizationBatch.R1202SixBlock
