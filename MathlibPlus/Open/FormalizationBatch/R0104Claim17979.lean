import MathlibPlus.Open.FormalizationBatch.R0104Claim17981

namespace MathlibPlus.Open.FormalizationBatch.R0104Claim17979

/-- The ordered-fibre statement uses the reviewed same-locator construction:
the retained relative-logarithmic coordinates form the vector first jet, while
complementary-divisor exchange kills only its scalar sum. -/
def claim17979_orderedFibersRetainInformation : Prop :=
  MathlibPlus.Open.FormalizationBatch.R0104Claim17981.claim17981_prematureScalarNoGo

end MathlibPlus.Open.FormalizationBatch.R0104Claim17979
